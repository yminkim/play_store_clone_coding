<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= list[0].title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "맑은고딕";
        }

        body {
            background-color: rgb(224, 224, 224);
        }

        main {
            width: 50%;
            background-color: #fff;
            margin: 8% auto;
            display: flex;
            flex-direction: column;
            border-radius: 5px;
            padding: 20px;
        }

        .post-area {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .post-title {
            font-size: 24px;
            font-weight: 400;
            margin-bottom: 15px;
        }

        .write-info-box {
            display: flex;
            margin-bottom: 20px;
        }

        .info-area-a {
            width: 40px;
            height: 40px;
            border-radius: 25px;
            overflow: hidden;
            margin-right: 10px;
        }

        .info-area-a img {
            width: 100%;
            height: 100%;
        }

        .info-area-b {
            font-size: 14px;
        }

        .post-date {
            color: rgb(155, 155, 155);
        }

        .content-area {
            border-top: 1px solid rgb(218, 218, 218);
            border-bottom: 1px solid rgb(218, 218, 218);
            padding: 10px 5px;
            margin-bottom: 20px;
        }


        /* 버튼 */
        .sub-btn {
            margin-top: auto;
            margin-left: auto;
            display: flex;
        }

        .sub-btn button, .sub-btn a {
            border: none;
            border-radius: 5px;
            
            padding: 10px 15px;
            cursor: pointer;
        }

        .submit-btn {
            background-color: rgb(160, 192, 229);
            color: rgb(0, 132, 255);
            font-weight: 700;
            margin-right: 10px;
        }

        .sub-btn a {
            background-color: rgb(224, 224, 224);
            color: black;
            margin-right: 10px;
            text-decoration: none;
        }

        .delete-btn {
            background-color: rgb(255, 177, 177);
            color: rgb(255, 17, 17);
            font-weight: 700;
        }


    </style>
</head>
<body>
    
    <main>
        <div class="post-area">
            <div class="post-title"><%= list[0].title %></div> 
            <div class="write-info-box">
                <div class="info-area-a">
                    <img src="../img/eaBanner.png" alt="">
                </div>
                <div class="info-area-b">
                    <div class="post-writer"><%= list[0].author %></div>
                    <div class="post-date"><%= list[0].regdate.getFullYear() %>-<%= list[0].regdate.getMonth() +1%>-<%= list[0].regdate.getDate() %> <%= list[0].regdate.getHours() %>:<%= list[0].regdate.getMinutes() %></div>
                </div>
            </div> 
            <div class="content-area"><%- list[0].content.replace(/\n/g, '<br>') %></div>
        </div>

        <div class="sub-btn">
            <a href="/list">취소</a>
            <button class="submit-btn" onclick="location.href='/edit/<%=list[0].id%>'">수정</button>
            <button class="delete-btn" onclick="location.href='/delete/<%=list[0].id%>'">삭제</button>
        </div>

    </main>

    
   
</body>
</html>